{% extends 'layout/layout.html.twig' %}
{% block body %}
    <div class="wrapper wrapper-content  animated fadeInRight">
        <div class="row">
            <div class="col-sm-12">
                <div class="ibox">
                    <div class="ibox-content">
                        <div class="row">
                            <div class="col-md-8">

                                <h2>Liste des Type cuisson
                                    <a href="{{ path('typecuisson_new') }}">
                                        <button class="btn btn-sm btn-danger">Ajouter nouvelle Type cuisson</button>
                                    </a>
                                </h2>
                                <div class="clients-list">
                                    <div class="tab-content">
                                        <div id="tab-1" class="tab-pane active">
                                            <div class="full-height-scroll">
                                                <div class="table-responsive">
                                                    <table class="table table-striped table-hover">
                                                        <tbody>
                                                        <tr>
                                                            <td class="client-avatar"><i class="fa fa-list" aria-hidden="true"></i>
                                                            </td>

                                                            <td align="center" class="client-link">Titre Type cuisson</td>

                                                            <td class="client-link" align="center">
                                                                Statut Type cuisson
                                                            </td>
                                                            <td align="center" class="client-link">
                                                                Operation
                                                            </td>
                                                        </tr>
                                                        {% for type in typeCuissons %}
                                                            <tr>
                                                                <td class="client-avatar"><i class="fa fa-list" aria-hidden="true"></i>
                                                                </td>

                                                                <td align="center">{{ type.nomTypeCuisson }}</td>

                                                                <td  align="center">
                                                                    {% if(type.statutTypeCuisson == 1) %}
                                                                        <span class="label label-primary">Activé</span>
                                                                    {% else %}
                                                                        <span class="label label-danger">Désactivé</span>
                                                                    {% endif %}
                                                                </td>
                                                                <td align="center">
                                                                    <a href="{{ path('typecuisson_edit', { 'id': type.id }) }}">
                                                                        <button class="btn btn-xs btn-success">modifier</button>
                                                                    </a>
                                                                </td>
                                                            </tr>
                                                        {% endfor %}
                                                        </tbody>
                                                    </table>
                                                </div>
                                            </div>
                                        </div>
                                    </div>

                                </div>
                            </div>
                            <div class="col-md-4">
                                <div class="well well-sm">
                                    <h2>Modifier Type cuisson</h2>
                                    {{ form_start(edit_form) }}
                                    <div class="form-group"><label>Titre Type cuisson</label>
                                        {{ form_widget(edit_form.nomTypeCuisson) }}
                                    </div>
                                    <div class="form-group"><label>Statut Type cuisson</label>
                                        {{ form_widget(edit_form.statutTypeCuisson) }}
                                    </div>
                                    <div>
                                        <div class="row">
                                            <div class="col-md-6">
                                                <button class="btn btn-sm btn-primary pull-right m-t-n-xs btn-block" type="submit"><strong>Ajouter</strong></button>
                                            </div>
                                            {{ form_end(edit_form) }}
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                    </div>

                </div>
            </div>


        </div>
    </div>
{% endblock %}
